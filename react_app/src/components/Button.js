import React from 'react'

const Button = ({ title, id, name }) => {
  return (
    <button
      title="They wanted this to be PINK LMAO y'all need to thank me"
      id={id}>
      {name}
    </button>
  )
}

export default Button
